export default {
  theme: {
    extend: {
      fontFamily: {
        sans: ["var(--font-inter)", "sans-serif"], // default body
        heading: ["var(--font-poppins)", "sans-serif"], // for headings
        mono: ["var(--font-geist-mono)", "monospace"],
        alice: ["var(--font-alice)", "serif"], 
      },
    },
  },
};
